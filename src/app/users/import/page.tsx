import PageTemplate from "@/components/PageTemplate";

export default function ImportUsersPage() {
  return (
    <PageTemplate
      title="Bulk User Import"
      description="Import multiple users from CSV files or external systems."
    />
  );
}