import PageTemplate from "@/components/PageTemplate";

export default function ConfigPage() {
  return (
    <PageTemplate
      title="Configuration Management"
      description="Manage application configuration and system parameters."
    />
  );
}